{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Phase Reviewer Output",
  "description": "Structured output from the phase-reviewer agent",
  "type": "object",
  "required": ["checklist", "gate", "next_phase", "status_set_to"],
  "properties": {
    "checklist": {
      "type": "object",
      "description": "Verification that all required steps were completed",
      "required": [
        "read_code_review",
        "checked_learnings",
        "verified_next_phase",
        "applied_updates"
      ],
      "properties": {
        "read_code_review": { "type": "boolean" },
        "checked_learnings": { "type": "boolean" },
        "verified_next_phase": { "type": "boolean" },
        "applied_updates": { "type": "boolean" }
      }
    },
    "gate": {
      "enum": ["GO", "UPDATE", "BLOCK"],
      "description": "Gate decision for proceeding to next phase"
    },
    "status_set_to": {
      "enum": ["EXECUTING_PHASE_N", "BLOCKED"],
      "description": "Status set in main.md (EXECUTING_PHASE_N for GO/UPDATE, BLOCKED for BLOCK)"
    },
    "next_phase": {
      "type": "integer",
      "minimum": 1,
      "description": "Which phase is next"
    },
    "learnings_applied": {
      "type": "array",
      "description": "Learnings from code review that were applied",
      "items": { "type": "string" }
    },
    "plan_updated": {
      "type": "boolean",
      "description": "Whether the plan was updated based on learnings"
    },
    "blocker": {
      "type": "string",
      "description": "Description of blocker if gate is BLOCK"
    },
    "summary": {
      "type": "string",
      "description": "Brief summary"
    }
  }
}
